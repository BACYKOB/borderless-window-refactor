# TASKS — поэтапные задачи рефакторинга BorderlessWindow

> Каждую задачу закрывает отдельный агент-исполнитель. Работу проверяет агент-ревьюер (см. «Процесс» внизу).
> Перед началом ЛЮБОЙ задачи прочитай: `AGENT_LOG.md` → `PLAN.md` → раздел своей задачи здесь. Больше ничего читать целиком не нужно — точечные чтения по указателям из METHOD_MAP.
> Статусы: `TODO` / `IN PROGRESS (кто)` / `REVIEW` / `DONE`. Исполнитель обновляет статус и добавляет запись в `AGENT_LOG.md` (раздел «Журнал сессий») при старте и при сдаче.

---

## T1 — METHOD_MAP: полная разметка исходника — `TODO`

**Цель**: единственный раз дочитать исходник и построить карту, чтобы никакой следующий агент не читал 5546 строк заново.

**Вход**: `original/BorderlessWindow.cs`. Строки 1–4876 уже прочитаны предыдущим агентом — конспект в `AGENT_LOG.md` («Рабочие заметки»). Осталось детально: 4876–5546.

**Сделать**:
1. Дочитать строки 4876–5546.
2. Создать `refactor/METHOD_MAP.md` — таблица ВСЕХ членов класса (константы-флаги, поля, свойства, методы, P/Invoke, структуры) со столбцами:
   `строки` | `имя` | `назначение (1 строка)` | `куда` (имя partial-файла из PLAN.md Часть 1 ИЛИ `DELETE` с причиной-пунктом из PLAN.md Часть 2) | `кто вызывает` (для нетривиальных).
3. Для каждого кандидата на удаление — грэп по исходнику: подтвердить, что его не вызывает живой код. Расхождения с PLAN.md Часть 2 зафиксировать в METHOD_MAP пометкой `KEEP (причина)`.

**Границы**: НЕ писать никакого кода. Только чтение, грэп, METHOD_MAP.md.

**Приёмка (ревьюер)**: выборочно 10 методов из разных зон файла — строки/назначение/адресат совпадают с исходником; все флаги из карты флагов handoff'ов присутствуют; каждый `DELETE` имеет причину; сумма «перенести + удалить» покрывает файл без дыр (по диапазонам строк).

---

## T2 — Interop + ядро (`BorderlessWindow.Interop.cs`, `BorderlessWindow.cs`) — `TODO`

**Вход**: METHOD_MAP.md (T1 = DONE). Состав файлов — PLAN.md Часть 1.

**Сделать**: создать `refactor/Controls/BorderlessWindow.Interop.cs` и `refactor/Controls/BorderlessWindow.cs`. Перенос 1:1 по METHOD_MAP, минус удаления (диагностика, ControlzEx, мёртвые флаги — PLAN.md Часть 2). В ядре — единая карта const-флагов с комментарием, какой partial использует каждый флаг + добавить два новых флага `EnableSeamGapFix = true`, `EnableLeftEdgeGhostGuard = true` (сами фиксы — T3/T4).

**Границы**: НЕ переносить методы, размеченные в другие partial'ы. `WindowProc` остаётся в ядре как диспетчер; тела обработчиков — заглушки НЕ создавать, просто вызовы методов, которые появятся в других partial'ах (компиляция всё равно проверяется только после T5).

**Приёмка**: баланс `{}`; каждый член из METHOD_MAP с адресатом «ядро»/«Interop» присутствует; ни одного `TsLog|SnapLog|WindowChromeBehavior|ControlzEx` вхождения; никакой метод не переименован.

---

## T3 — Chrome + фикс призрака (`BorderlessWindow.Chrome.cs`) — `TODO`

**Вход**: METHOD_MAP.md; PLAN.md Часть 4 (диагноз и решение); `handoffs/resize-ghost.md` (доказанные тупики — не повторять).

**Сделать**: перенос chrome-методов по METHOD_MAP + внедрить `EnableLeftEdgeGhostGuard`:
- в `ThemedFrameNcCalcSize` — 1px NC-inset слева, симметрично существующему верхнему `ThemedFrameInset` (тот же порядок применения относительно клампов к work-area);
- в `ApplyThemedBorderMetrics` — при активном флаге `BorderThickness.Left = 0` из кода (XAML: `BorderThickness="1,0,1,1"` в `xaml/MainWindow.xaml`, триггер Maximized → 0 — сохранить поведение: в maximized inset тоже 0);
- при `EnableLeftEdgeGhostGuard=false` — поведение бит-в-бит как в исходнике.

**Границы**: hit-test НЕ менять; maximized/fullscreen пути НЕ менять; только левый inset + компенсация толщины.

**Приёмка**: fallback-ветка идентична исходнику (diff-сверка логики); inset применяется до/после клампов в том же порядке, что top-inset; `ThemeBorderColorRef` не изменён.

---

## T4 — SnapResize + фикс зазора (`BorderlessWindow.SnapResize.cs`) — `TODO`

**Вход**: METHOD_MAP.md; PLAN.md Часть 3; `handoffs/snap-joint-resize.md`, `handoffs/resize-jitter.md`; лог бага `logs/snap-joint-resize-gap.log`.

**Сделать**: перенос snap-методов + внедрить `EnableSeamGapFix`:
1. DivGrip-путь: вместо предиктивного реального сдвига соседей (`ApplyDividerBatch(moveOur=false)` + финальный) — предвычисление клампа `effDiv` через `WM_GETMINMAXINFO` соседа и ОДИН атомарный `BeginDefer/EndDeferWindowPos` (мы + соседи + co-tiles). Существующий realign на отпускании (`ArmDivReleaseRealign`) сохранить.
2. Frame-resize путь: «grower-first» — когда сосед растёт, коммитить его новый край в `WM_WINDOWPOSCHANGING` (до нашего кадра); когда сосед сжимается — как сейчас, после `WM_WINDOWPOSCHANGED`. Инвариант: переходное состояние всегда перекрытие, никогда зазор.
3. При `EnableSeamGapFix=false` — прежняя двухпроходная схема без изменений.

**Границы**: PassiveFollow, tile-group, free-edge grip, snap-follow таймер — перенос 1:1 без изменений; `CaptionUnsnapRestoreThresholdDip` не трогать (он в Unsnap).

**Приёмка**: fallback идентичен исходнику; в новом пути наше окно двигается ровно один раз за кадр (нет двойного move → нет ghost-trail регрессии); min-size clamp обработан ДО коммита; обработчик в `WM_WINDOWPOSCHANGING` не конфликтует с существующей pending-rect логикой ядра.

---

## T5 — Unsnap + Animation + Taskbar — `TODO`

**Вход**: METHOD_MAP.md. Состав — PLAN.md Часть 1.

**Сделать**: `BorderlessWindow.Unsnap.cs`, `BorderlessWindow.Animation.cs`, `BorderlessWindow.Taskbar.cs`. Чистый перенос 1:1 минус удаления (в Animation НЕ переносить старую серую маску `StartMaskCrossfade`/`MaskFadeLoop`/`Ease` — но сначала грэп-подтверждение из METHOD_MAP, что они мертвы).

**Границы**: НИКАКИХ изменений логики. Unsnap Variant A/A+/A++ — байт-в-байт (минус логи).

**Приёмка**: баланс скобок; все методы из METHOD_MAP на месте; `CaptionUnsnapRestoreThresholdDip = 20.0` не изменён; WinEvent-hook цепочка (`MaybeSuppressUnsnapResnapFrame` → `OnUnsnapWinEvent` → `FinishUnsnapWinEventRestore`) полная.

---

## T6 — Сквозная верификация + IDEAS.md + резюме — `TODO`

**Вход**: T2–T5 = DONE.

**Сделать**:
1. Полнота: каждый член METHOD_MAP либо найден грэпом в `Controls/*.cs`, либо помечен DELETE. Список расхождений = пусто.
2. Целостность: каждый вызываемый метод имеет объявление; удалённые символы — 0 вхождений; неиспользуемые P/Invoke после чисток удалены; using'и согласованы во всех 7 файлах; один и тот же namespace/имя класса везде.
3. Баланс `{}`/`()` каждого файла.
4. Написать `refactor/IDEAS.md`: эскалация ghost-guard (динамический 2px inset при left-drag), транзитивная 4-оконная группа, удаление ControlzEx PackageReference из csproj (руками пользователя), возможный вынос флагов в настройки, + всё накопленное в журнале сессий.
5. Финальное резюме пользователю: что удалено, как выключить каждый фикс (какая константа в каком файле), 7 сценариев ручного теста (PLAN.md Часть 5 п.6).

**Приёмка**: чек-листы 1–3 приложены к сдаче с фактическими числами (кол-во методов, грэп-выхлопы), не «на словах».

---

## Процесс

- **Исполнитель**: берёт ОДНУ задачу в статусе `TODO` (строго по порядку T1→T6), ставит `IN PROGRESS`, по завершении — `REVIEW` + запись в журнал `AGENT_LOG.md`: что сделал, что выяснил нового (обновить «Рабочие заметки», если исходник разошёлся с ожиданиями), какие решения принял сам и почему.
- **Ревьюер** (отдельная сессия): проверяет по «Приёмке» задачи, пишет вердикт в журнал; `DONE` или возврат в `IN PROGRESS` со списком замечаний.
- **Правила для всех**: не редактировать `original/` и `handoffs/`; не переименовывать публичные/используемые в XAML сущности; экономить кредиты — точечные чтения по METHOD_MAP вместо перечитывания исходника; писать файлы крупными Write-блоками; вопросы пользователю — только если блокер.
- **Компиляция**: в песочнице нет C#-компилятора. Финальную сборку на `net10.0-windows` и ручные тесты делает пользователь ПОСЛЕ T6.
